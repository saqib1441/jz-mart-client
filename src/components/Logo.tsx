import { FC } from "react";

const Logo: FC = () => {
  return (
    <div>
      <h1 className="sm:text-4xl text-2xl font-inter font-bold">
        <span className="text-primary">JZ</span> MART
      </h1>
    </div>
  );
};

export default Logo;
